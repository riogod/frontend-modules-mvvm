/**
 * Типы для расширенного манифеста модулей.
 *
 * На текущий момент формат манифеста совпадает с существующим Module.
 * Файл оставлен для возможного расширения без затрагивания основного контракта.
 */

import type { Module } from '../../../../modules/interface';

/**
 * Переиспользуем существующий тип Module как контракт манифеста.
 */
export type ManifestModule = Module;


