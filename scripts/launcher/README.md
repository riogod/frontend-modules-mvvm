# Launcher - CLI для разработки MFE приложения

Интерактивный инструмент командной строки для управления конфигурациями запуска и модулями MFE приложения.

## Быстрый старт

```bash
npm start
# или
npm run dev
```

## Основные возможности

- 🚀 **Запуск конфигураций** - Быстрый запуск приложения с предустановленными настройками
- ⚙️ **Управление конфигурациями** - Создание, редактирование и удаление конфигураций
- 📦 **Управление модулями** - Настройка источников модулей (локальные/удаленные)
- 🛠️ **Генерация модулей** - Создание новых MFE модулей с готовой структурой
- 🔧 **Глобальные настройки** - Настройка Remote Server URL и API URL

---

## Главное меню

При запуске `npm start` отображается главное меню:

```
🚀 Frontend MFE Launcher

Выберите действие:
  1. Development (2 модулей) [используется: 15 раз] ★
  2. Staging (1 модулей) [используется: 5 раз]
  → Создать новую конфигурацию
  → Создать новый MFE модуль
  → Глобальные настройки
  → Выход
```

### Список конфигураций

В верхней части меню отображаются сохраненные конфигурации с информацией:

- **Номер** - Порядковый номер в списке
- **Имя** - Название конфигурации
- **Количество модулей** - Сколько модулей включено
- **Счетчик использования** - Сколько раз была запущена
- **★** - Последняя использованная конфигурация
- **Описание** - Описание конфигурации (если задано)

### Команды

В нижней части меню отображаются доступные команды:

- **Создать новую конфигурацию** - Создание новой конфигурации запуска
- **Создать новый MFE модуль** - Генерация нового модуля
- **Глобальные настройки** - Настройка Remote Server URL и API URL
- **Выход** - Завершение работы

---

## Работа с конфигурациями

### Выбор конфигурации

Выберите конфигурацию из списка, чтобы увидеть меню действий:

```
Конфигурация: "Development"

Модули:
  🟢 todo: LOCAL ✅ моки
  🟢 api_example: LOCAL ✅ моки

Настройки конфигурации:
  Уровень логирования: INFO
  Использовать локальные моки: Да
  API URL: ⚠️ не настроен

Что сделать?
  → Запустить
  → Редактировать модули
  → Настроить параметры
  → Удалить
  → Назад
```

### Запуск конфигурации

Выберите **"Запустить"** для запуска приложения с выбранной конфигурацией.

При запуске:

1. Генерируется манифест модулей
2. Запускается dev-server на порту 1337
3. Запускается Vite dev server на порту 4200

### Редактирование модулей

Выберите **"Редактировать модули"** для настройки источников модулей.

#### Типы источников модулей

- **🟢 LOCAL** - Модуль загружается из локальной директории `packages/`
- **🔵 REMOTE** - Модуль загружается с удаленного сервера (требует настройки Remote Server URL)
- **🟣 REMOTE_CUSTOM** - Модуль загружается по кастомному URL
- **⏭️ Пропустить** - Модуль не загружается

#### Настройка модуля

1. Выберите модуль из списка
2. Выберите источник (LOCAL/REMOTE/REMOTE_CUSTOM/Пропустить)
3. Если выбран REMOTE_CUSTOM, введите URL до `remoteEntry.js`
4. Выберите, использовать ли локальные моки для этого модуля

### Настройка параметров

Выберите **"Настроить параметры"** для изменения настроек конфигурации:

- **Уровень логирования** - NONE, ERROR, WARN, INFO, DEBUG, TRACE
- **Использовать локальные моки** - Использовать MSW моки или удаленный сервис
- **API URL** - URL API сервера (если моки отключены)

**Примечание:** Если задана переменная окружения `LOG_LEVEL`, она имеет приоритет над настройкой конфигурации.

### Удаление конфигурации

Выберите **"Удалить"** и подтвердите удаление.

---

## Создание конфигурации

1. Выберите **"Создать новую конфигурацию"** из главного меню
2. Настройте модули (см. [Редактирование модулей](#редактирование-модулей))
3. Введите имя конфигурации
4. Введите описание (необязательно)
5. Настройте параметры:
   - Уровень логирования
   - Использование локальных моков
   - API URL (если моки отключены)
6. Выберите, запустить ли конфигурацию сразу

---

## Создание MFE модуля

1. Выберите **"Создать новый MFE модуль"** из главного меню
2. Следуйте инструкциям интерактивного мастера:
   - Введите название модуля (kebab-case)
   - Выберите опции генерации
3. После создания модуль будет готов к использованию

**Важно:** После создания модуля добавьте его в `host/src/modules/modules.ts`

---

## Глобальные настройки

Выберите **"Глобальные настройки"** для настройки:

### Remote Server URL

URL удаленного сервера для загрузки REMOTE модулей.

**Формат:** `http://localhost:3000`

**Использование:** Используется для автоматического формирования URL модулей:

```
{remoteServerUrl}/modules/{moduleName}/latest/remoteEntry.js
```

### API URL (глобальный fallback)

URL API сервера, который используется, если в конфигурации не задан `apiUrl`.

**Формат:** `http://localhost:3001/back/`

---

## Аргументы командной строки

### Запуск с последней конфигурацией

```bash
npm start -- --last
# или
npm start -l
```

### Запуск с указанной конфигурацией

```bash
npm start -- --config Development
# или
npm start -c Development
```

### Создание модуля

```bash
npm start -- --create-module
```

---

## Структура конфигурации

Конфигурации сохраняются в `.launcher/configs.json`:

```json
{
  "version": "1.0.0",
  "lastUsed": "development",
  "remoteServerUrl": "http://localhost:3000",
  "configurations": {
    "development": {
      "name": "Development",
      "description": "Все модули локально",
      "modules": {
        "todo": {
          "source": "local",
          "path": "packages/todo",
          "priority": 1,
          "useLocalMocks": true
        }
      },
      "settings": {
        "logLevel": "INFO",
        "useLocalMocks": true,
        "apiUrl": ""
      }
    }
  }
}
```

---

## Переменные окружения

### LOG_LEVEL

Уровень логирования (имеет приоритет над настройками конфигурации):

```bash
LOG_LEVEL=DEBUG npm start
```

Доступные значения: `NONE`, `ERROR`, `WARN`, `INFO`, `DEBUG`, `TRACE`

### VITE_USE_LOCAL_MOCKS

Использовать локальные моки (имеет приоритет над настройками):

```bash
VITE_USE_LOCAL_MOCKS=false npm start
```

---

## Типы модулей

### INIT модули

Модули, которые загружаются всегда локально при инициализации:

- `core` - Базовый функционал
- `core.layout` - Компоненты макета

Эти модули не настраиваются в конфигурациях.

### NORMAL модули

Модули из директории `packages/`, которые можно настраивать:

- Источник (LOCAL/REMOTE/REMOTE_CUSTOM)
- Приоритет загрузки
- Использование локальных моков

---

## Советы и рекомендации

### Организация конфигураций

- **Development** - Все модули локально для разработки
- **Staging** - Смешанная конфигурация (часть локально, часть удаленно)
- **Production Test** - Все модули удаленно для тестирования

### Использование моков

- **Локальные моки (MSW)** - Для разработки без бэкенда
- **Удаленный сервис** - Для тестирования с реальным API

### Оптимизация производительности

- Используйте **LOCAL** модули для активной разработки (быстрый HMR)
- Используйте **REMOTE** модули для тестирования интеграции

---

## Устранение неполадок

### Модули не найдены

Если при создании конфигурации модули не найдены:

1. Убедитесь, что модули находятся в `packages/`
2. Проверьте структуру модуля (должен быть `package.json`)

### Конфигурация не запускается

1. Проверьте, что все LOCAL модули существуют
2. Проверьте настройки Remote Server URL (для REMOTE модулей)
3. Проверьте логи в консоли

### Манифест не генерируется

1. Убедитесь, что модули имеют правильную структуру
2. Проверьте файл `packages/{module}/src/config/module_config.ts`

---

## Файлы и директории

- `.launcher/configs.json` - Сохраненные конфигурации
- `.launcher/current-manifest.json` - Текущий манифест (генерируется при запуске)
- `packages/` - NORMAL модули
- `host/src/modules/` - INIT модули

---

## Поддержка

При возникновении проблем:

1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте структуру проекта

---

## Дополнительная информация

- [Документация для разработчиков](./README.dev.md)
- [План рефакторинга](../../../refactor_plan.md)
